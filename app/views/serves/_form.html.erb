<%= form_for(serf) do |f| %>
  <% if serf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serf.errors.count, "error") %> prohibited this serf from being saved:</h2>

      <ul>
      <% serf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :checkin_time %>
    <%= f.text_field :checkin_time %>
  </div>

  <div class="field">
    <%= f.label :checkout_time %>
    <%= f.text_field :checkout_time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
